import sentry_sdk
from sentry_sdk.integrations.django import DjangoIntegration

SENTRY_URL = 'https://dummy.ingest.sentry.io/12345'
sentry_sdk.init(dsn=SENTRY_URL, integrations=[DjangoIntegration()])

DEBUG = False

DB_USER = ''
DB_PASSWORD = ''
DB_HOST = 'localhost'
DB_PATH = ''  # mysql dump path for fabric

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'unilex',
        'USER': DB_USER,
        'PASSWORD': DB_PASSWORD
    },
    'medd': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'medd',
        'USER': DB_USER,
        'PASSWORD': DB_PASSWORD
    }
}
ALLOWED_HOSTS = ['localhost', '...']
DEFAULT_FROM_EMAIL = "noreply@example.com"
ADMINS = MANAGERS = [('Admin', 'admin@example.com'),]
SECRET_KEY = 'foobar'
